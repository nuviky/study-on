{% extends 'base.html.twig' %}

{% block title %}{{course.name}}{% endblock %}

{% block body %}
    <a href="{{ path('course_index') }}">Вернуться к списку программ</a>
    <h1>{{course.name}}</h1>
    <div>{{course.description}}</div>
    <div>Программа обучения:</div>
    {% for lesson in course.lessons|sort((a, b) => a.lessonNumber <=> b.lessonNumber)%}
        <p><a href="{{ path('lesson_show', {'id': lesson.id}) }}">{{ lesson.lessonNumber }}. {{ lesson.title }}</a></p>
    {% else %}
        <div>Уроки для данной программы обучения в разработке</div>
    {% endfor%}

    <a href="{{ path('course_edit', {'id': course.id}) }}">Редактировать программу</a>
    <a href="{{ path('lesson_new', {'id': course.id}) }}">Добавить урок</a>
    {{ include('course/_delete_form.html.twig') }}
{% endblock %}
